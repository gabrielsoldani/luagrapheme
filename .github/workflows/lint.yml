name: Lint

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup 'lua'
        uses: leafo/gh-actions-lua@v10
        with:
          luaVersion: 5.4.6

      - name: Setup 'luarocks'
        uses: leafo/gh-actions-luarocks@v4

      - name: Setup 'luacheck'
        run: luarocks install luacheck

      - name: Run 'make lint'
        run:
          make CLANG_FORMAT="clang-format-15" lint
